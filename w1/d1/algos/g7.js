/* 

  Members: Derek, Henry, Justin

  Create a singly linked list with the following methods:

  -
  insertToBack
  -
  seedFromArray
    - convert given array to linked list
  - print
    - print a comma separated string of your linekd list data
  
  Bonus:
    - how would you design the linked list so that you can add a new node to the back in O(1) constant time (no looping)
*/

class Node {
    def __init__(self, data):
        self.data = data
    self.next = None
}
class List:
    def insertToBack(self, value):
    new_val = value
if self.head == None:
    self.head = new_val
return


end = self.head
while (end.next):
    end = end.next
runner.next == null
self.head == runner
node == self.head
end.next = new_val
self.head = last
// Members: Megan, Roza, Eddy

// // You have a Node, and a List
// // Nodes have a next, which takes you to the next node
// // Lists have a head, which points to the first node of the list

// Create a singly linked list with the following methods:

// -
// insertToBack
// -
// seedFromArray
//   - convert given array to linked list
// - print
//   - print a comma separated string of your linekd list data

// Bonus:
//   - how would you design the linked list so that you can add a new node to the back in O(1) constant time (no looping)



Class SLL:
    def__init__(self)
self.head = newNode

Class SLLNode:
    def__init__(self, val):
    self.data = val
self.next = none


    .newNode = SlLNode(val)
neNode.next = self.head
self.head = newNode


Class Slist {

    Constructor(head = null) {
        this.head = head;

    }
}

Class dlnode {
    Constructor(data) {
        this.data = data;
        this.next = null;
        runner = this.head;
    }

}
}